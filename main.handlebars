<html>
<head>
	<title> Main Handlebars </title>
	<style type="text/css">
		* {
			margin: 0px;
			padding: 0px;
			box-sizing: border-box;
		}
		body { /* The css here is really good to be used on any site */
			/* If the first font doesn't exist or sint avaliable to the user the one after the comma is taken */
			font-family: 'Lato', 'Arial Black', 'Comic Sans';
			font-weight: 400;
			font-size: 16px;
			line-height: 1.7;
			color: #777;
		}
	</style>
</head>

<body>
	<h1> Hello Handlebars </h1>
	{{#if published}}
		{{content}}
	{{/if}}

	<ul>
		{{#each people}}
			<li> {{this}} </li>
		{{/each}}
	</ul>

	<h4> {{person}} ...showed only because you are under the /person/{{person}} route </h4>
</body>
</html>